# My App - AGENTS.md

## Stack

TypeScript / Next.js / Prisma

## Commands

- `pnpm build` — compile
- `pnpm test` — run tests
- `pnpm lint` — eslint

## Architecture

- `src/app/` — pages
- `src/components/` — React components
